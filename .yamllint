extends: default

rules:
  # 80 chars should be enough, but don't fail if a line is longer
  line-length:
    max: 400
    level: warning

  indentation: disable
  document-start: disable
  brackets: disable
